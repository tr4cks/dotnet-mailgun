{
  "name": "dotnet-mailgun",
  "version": "1.0.5",
  "scripts": {
    "prepare": "husky install",
    "format": "dotnet format && prettier --write ."
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  },
  "devDependencies": {
    "@prettier/plugin-xml": "^1.1.0",
    "husky": "^7.0.4",
    "lint-staged": "^12.1.2",
    "prettier": "2.5.0"
  }
}
